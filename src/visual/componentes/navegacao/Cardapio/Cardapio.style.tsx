import { styled, Menu } from "@mui/material";

export const CardapioConteiner = styled("div")``;

export const CardapioMenu = styled(Menu)`
    .MuiList-root {
        padding: 0;
    }

    .MuiPaper-root {
        width: 310px;
    }
    transform: translateY(-13px);
`;
